--- src/epiphany-pantheon/debian/patches/99_tabs_dont_expand.patch	2016-10-24 02:12:05.318526000 +0900
+++ 99_tabs_dont_expand.patch.remake	2016-10-24 02:22:18.409816601 +0900
@@ -1,6 +1,6 @@
 --- a/src/ephy-notebook.c
 +++ b/src/ephy-notebook.c
-@@ -37,7 +37,7 @@
+@@ -35,7 +35,7 @@
  #include <glib/gi18n.h>
  #include <gtk/gtk.h>
  
@@ -9,26 +9,17 @@
  
  #define AFTER_ALL_TABS -1
  #define NOT_IN_APP_WINDOWS -2
-@@ -630,11 +630,11 @@
+@@ -670,11 +670,11 @@
  
- 	/* setup load feedback */
- 	spinner = gtk_spinner_new ();
--	gtk_box_pack_start (GTK_BOX (hbox), spinner, FALSE, FALSE, 0);
-+	gtk_box_pack_end (GTK_BOX (box), spinner, FALSE, FALSE, 0);
+   /* setup load feedback */
+   spinner = gtk_spinner_new ();
+-  gtk_box_pack_start (GTK_BOX (hbox), spinner, FALSE, FALSE, 0);
++  gtk_box_pack_end (GTK_BOX (box), spinner, FALSE, FALSE, 0);
+ 
+   /* setup site icon, empty by default */
+   icon = gtk_image_new ();
+-  gtk_box_pack_start (GTK_BOX (hbox), icon, FALSE, FALSE, 0);
++  gtk_box_pack_end (GTK_BOX (box), icon, FALSE, FALSE, 0);
+   /* don't show the icon */
  
- 	/* setup site icon, empty by default */
- 	icon = gtk_image_new ();
--	gtk_box_pack_start (GTK_BOX (hbox), icon, FALSE, FALSE, 0);
-+	gtk_box_pack_end (GTK_BOX (box), icon, FALSE, FALSE, 0);
- 	/* don't show the icon */
- 
- 	/* Set minimal size */
-@@ -741,7 +741,7 @@
- 
- 	gtk_container_child_set (GTK_CONTAINER (notebook),
- 				 GTK_WIDGET (embed),
--				 "tab-expand", TRUE,
-+				 "tab-expand", FALSE,
- 				 NULL);
- 
- 	if (jump_to)
+   /* Set minimal size */
